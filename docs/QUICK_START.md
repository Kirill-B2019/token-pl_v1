# 🚀 Быстрый старт

Это руководство поможет вам быстро развернуть и начать работу с Токен Платформой.

## Предварительные требования

- **PHP**: 8.2 или выше
- **Composer**: 2.x
- **Node.js**: 18.x или выше (для Vue.js интерфейса)
- **MySQL/PostgreSQL/SQLite**: для базы данных
- **Redis**: для кеширования (опционально)

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-org/token-platform.git
cd token-platform
```

### 2. Установка зависимостей

```bash
# PHP зависимости
composer install

# Node.js зависимости (для Vue.js)
npm install
```

### 3. Настройка окружения

```bash
# Копирование файла конфигурации
cp .env.example .env

# Генерация ключа приложения
php artisan key:generate
```

### 4. Настройка базы данных

Отредактируйте `.env` файл:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=token_platform
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

### 5. Запуск миграций

```bash
# Запуск миграций
php artisan migrate

# (Опционально) Заполнение тестовыми данными
php artisan db:seed
```

### 6. Настройка платежной системы 2can

```env
TWOCAN_SHOP_ID=1337
TWOCAN_SECRET_KEY=your_secret_key_here
TWOCAN_API_URL=https://2can.ru/api/v1/
```

### 7. Запуск сервера

```bash
# Laravel сервер
php artisan serve

# Vue.js dev сервер (в отдельном терминале)
npm run dev
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000`

## Первый вход

### Создание администратора

```bash
php artisan tinker

# Внутри tinker:
User::create([
    'name' => 'Администратор',
    'email' => 'admin@token-platform.local',
    'password' => Hash::make('password'),
    'role' => 'admin'
]);
```

### Доступ к админ-панели

1. Перейдите на `http://127.0.0.1:8000/login`
2. Войдите как администратор
3. Перейдите в админ-панель через меню

## Проверка работоспособности

### 1. Тестирование платежей

```bash
# Запуск платежного сервера в фоне
php artisan serve --host=127.0.0.1 --port=8000

# В отдельном терминале тестирование API
curl -X GET http://127.0.0.1:8000/api/health
```

### 2. Запуск тестов

```bash
# Unit и Feature тесты
php artisan test

# С покрытием кода
php artisan test --coverage
```

## Следующие шаги

1. **Изучите документацию**:
   - [Руководство пользователя](USER_GUIDE.md)
   - [Руководство разработчика](DEVELOPER_GUIDE.md)

2. **Настройте продакшн**:
   - [Установка и настройка](INSTALLATION.md)
   - [Безопасность](SECURITY.md)

3. **Разверните на сервере**:
   - Настройте веб-сервер (Nginx/Apache)
   - Настройте SSL сертификаты
   - Настройте бэкапы

## Полезные команды

```bash
# Очистка кеша
php artisan cache:clear
php artisan config:clear
php artisan route:clear

# Создание нового пользователя
php artisan tinker
>>> User::factory()->create()

# Просмотр маршрутов
php artisan route:list

# Создание миграции
php artisan make:migration create_example_table

# Создание модели
php artisan make:model Example

# Создание контроллера
php artisan make:controller ExampleController
```
